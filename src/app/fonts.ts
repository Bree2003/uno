import { Raleway, Open_Sans, Nunito_Sans } from 'next/font/google';

export const raleway = Raleway({ subsets: ['latin'] });
export const nunitoSans = Nunito_Sans({ subsets: ['latin'] });
export const openSans = Open_Sans({ subsets: ['latin'] });
